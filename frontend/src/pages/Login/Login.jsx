import { Login } from '../../components/Auth';

/**
 * Página de Login
 * 
 * Página principal para el inicio de sesión de usuarios.
 * Renderiza el componente Login con toda la funcionalidad necesaria.
 */
const LoginPage = () => {
  return <Login />;
};

export default LoginPage;